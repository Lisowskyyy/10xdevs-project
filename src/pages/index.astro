---
import Layout from "../layouts/Layout.astro";
import { createServerSupabaseClient } from "../lib/supabase";

// Jeśli ktoś jest już zalogowany, niech idzie od razu do dashboardu
const supabase = createServerSupabaseClient(Astro.cookies);
const {
  data: { session },
} = await supabase.auth.getSession();
if (session) {
  return Astro.redirect("/dashboard");
}
---

<Layout title="VERANIMA | Cyfrowa Świątynia" showNav={false}>
  <!-- Floating Login Button -->
  <div class="fixed top-6 right-6 z-50">
    <a
      href="/login"
      class="px-6 py-3 rounded-full bg-gradient-to-r from-amber-500 to-orange-600 text-white font-bold hover:scale-105 transition-transform shadow-lg shadow-amber-500/20 backdrop-blur-sm"
    >
      Login
    </a>
  </div>

  <!-- Hero Section -->
  <section
    id="hero"
    class="min-h-screen flex flex-col items-center justify-center text-center px-4 relative overflow-hidden snap-start"
  >
    <div class="absolute inset-0 z-0">
      <div class="absolute inset-0" style="background-image: url('/veranima_background.png'); background-size: cover; background-position: center; background-repeat: no-repeat;"></div>
      <div class="bg-slate-950/70 absolute inset-0"></div>
    </div>

    <div class="relative z-10 max-w-3xl animate-fade-in-up">
      <h1
        class="text-6xl md:text-8xl font-serif font-bold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 via-yellow-500 to-amber-600 mb-6 drop-shadow-lg"
      >
        VERANIMA
      </h1>

      <p class="text-xl md:text-2xl text-gray-300 mb-12 font-light italic">
        "Twoja cyfrowa świątynia. Uwolnij umysł, spal to co zbędne, odnajdź spokój."
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-16">
        <a
          href="/signup"
          class="px-8 py-3 rounded-full bg-white/10 text-white border border-white/20 hover:bg-white/20 transition-colors font-semibold backdrop-blur-sm"
        >
          Stwórz Konto
        </a>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 animate-bounce">
      <div class="flex flex-col items-center gap-2 text-gray-400">
        <span class="text-sm font-light">Scroll to explore</span>
        <span class="text-2xl">↓</span>
      </div>
    </div>
  </section>

  <!-- Gratitude Section -->
  <section
    id="gratitude"
    class="h-screen flex flex-col items-center justify-center text-center px-4 relative overflow-hidden snap-start"
  >
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-indigo-900 via-violet-900 to-purple-900"></div>

    <div class="relative z-10 max-w-3xl">
      <div class="text-8xl mb-8 animate-glow">📖</div>
      <h2
        class="text-5xl md:text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-violet-200 to-indigo-300 mb-6"
      >
        Dziennik Wdzięczności
      </h2>
      <p class="text-xl md:text-2xl text-gray-200 mb-8 font-light leading-relaxed">
        Codzienny dziennik budujący Twoją wewnętrzną siłę.
        <br />
        Praktykuj wdzięczność i wzmacniaj pozytywne myśli.
      </p>
    </div>
  </section>

  <!-- Dracarys Section -->
  <section
    id="dracarys"
    class="h-screen flex flex-col items-center justify-center text-center px-4 relative overflow-hidden snap-start"
  >
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-slate-900 via-slate-800 to-slate-900"></div>


    <div class="relative z-10 max-w-3xl">
      <div class="text-8xl mb-8 dragon-icon">🐉</div>
      <h2
        class="text-5xl md:text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-orange-300 to-red-400 mb-6"
      >
        Dracarys Mode
      </h2>
      <p class="text-xl md:text-2xl text-gray-200 mb-8 font-light leading-relaxed">
        Wizualne spalanie negatywnych myśli i lęków.
        <br />
        Uwolnij się od tego, co Cię ogranicza.
      </p>
    </div>
  </section>

  <!-- Meditation Section -->
  <section
    id="meditation"
    class="h-screen flex flex-col items-center justify-center text-center px-4 relative overflow-hidden snap-start"
  >
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-teal-900 via-cyan-900 to-blue-900"></div>

    <div class="relative z-10 max-w-3xl">
      <div class="text-8xl mb-8 lotus-icon">🧘‍♂️</div>
      <h2
        class="text-5xl md:text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-teal-200 to-cyan-300 mb-6"
      >
        Kręgi Mocy
      </h2>
      <p class="text-xl md:text-2xl text-gray-200 mb-8 font-light leading-relaxed">
        Wspólna medytacja i intencje wspierające Twój rozwój.
        <br />
        Doświadcz przepływu energii i muzyki.
      </p>
    </div>
  </section>
</Layout>

<style>
  html {
    scroll-behavior: smooth;
    scroll-snap-type: y mandatory;
  }

  section {
    scroll-snap-align: start;
  }

  /* Fade in animation */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }

  /* Glow animation for book icon */
  @keyframes glow {
    0%,
    100% {
      filter: drop-shadow(0 0 10px rgba(139, 92, 246, 0.5));
      transform: scale(1);
    }
    50% {
      filter: drop-shadow(0 0 30px rgba(139, 92, 246, 0.8));
      transform: scale(1.05);
    }
  }
  .animate-glow {
    animation: glow 3s ease-in-out infinite;
  }

  /* Dragon hover glow */
  .dragon-icon {
    transition: all 0.3s ease;
    cursor: pointer;
  }
  .dragon-icon:hover {
    filter: drop-shadow(0 0 40px rgba(251, 146, 60, 0.8));
    transform: scale(1.1) rotate(5deg);
  }

  /* Lotus icon with radiating circles */
  .lotus-icon {
    position: relative;
    transition: all 0.3s ease;
  }
  .lotus-icon::before,
  .lotus-icon::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border: 2px solid rgba(94, 234, 212, 0.3);
    border-radius: 50%;
    animation: ripple 3s ease-out infinite;
  }
  .lotus-icon::before {
    width: 120px;
    height: 120px;
    animation-delay: 0s;
  }
  .lotus-icon::after {
    width: 180px;
    height: 180px;
    animation-delay: 1s;
  }
  @keyframes ripple {
    0% {
      transform: translate(-50%, -50%) scale(0.8);
      opacity: 1;
    }
    100% {
      transform: translate(-50%, -50%) scale(1.5);
      opacity: 0;
    }
  }
  .lotus-icon:hover {
    filter: drop-shadow(0 0 30px rgba(94, 234, 212, 0.6));
    transform: scale(1.1);
  }


  /* Responsive adjustments */
  @media (max-width: 768px) {
    section {
      padding: 2rem 1rem;
    }

    h1 {
      font-size: 3rem !important;
    }

    h2 {
      font-size: 2.5rem !important;
    }

    .fire {
      width: 40px !important;
      height: 60px !important;
    }
  }
</style>
