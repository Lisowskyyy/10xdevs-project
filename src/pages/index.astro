---
import Layout from "../layouts/Layout.astro";
import { createServerSupabaseClient } from "../lib/supabase";

// Jeśli ktoś jest już zalogowany, niech idzie od razu do dashboardu
const supabase = createServerSupabaseClient(Astro.cookies);
const {
  data: { session },
} = await supabase.auth.getSession();
if (session) {
  return Astro.redirect("/dashboard");
}
---

<Layout title="VERANIMA | Cyfrowa Świątynia" showNav={false}>
  <div class="min-h-screen flex flex-col items-center justify-center text-center px-4 relative overflow-hidden">
    <!-- Tło z gradientem (możesz dostosować kolory) -->
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-[#0f0c29] via-[#302b63] to-[#24243e] opacity-90"></div>

    <!-- Treść -->
    <div class="relative z-10 max-w-3xl animate-fade-in-up">
      <h1
        class="text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-amber-200 to-yellow-500 mb-6 drop-shadow-lg"
      >
        VERANIMA
      </h1>

      <p class="text-xl md:text-2xl text-gray-300 mb-12 font-light italic">
        "Twoja cyfrowa świątynia. Uwolnij umysł, spal to co zbędne, odnajdź spokój."
      </p>

      <!-- Karty Funkcji -->
      <div class="grid md:grid-cols-3 gap-6 mb-16 text-left">
        <div class="bg-white/5 p-6 rounded-xl border border-white/10 hover:border-amber-500/50 transition-colors">
          <div class="text-3xl mb-3">🐉</div>
          <h3 class="text-lg font-bold text-amber-100 mb-2">Dracarys</h3>
          <p class="text-sm text-gray-400">Wizualne spalanie negatywnych myśli i lęków.</p>
        </div>

        <div class="bg-white/5 p-6 rounded-xl border border-white/10 hover:border-green-500/50 transition-colors">
          <div class="text-3xl mb-3">💚</div>
          <h3 class="text-lg font-bold text-green-100 mb-2">Wdzięczność</h3>
          <p class="text-sm text-gray-400">Codzienny dziennik budujący Twoją wewnętrzną siłę.</p>
        </div>

        <div class="bg-white/5 p-6 rounded-xl border border-white/10 hover:border-blue-500/50 transition-colors">
          <div class="text-3xl mb-3">🧘‍♂️</div>
          <h3 class="text-lg font-bold text-blue-100 mb-2">Medytacja</h3>
          <p class="text-sm text-gray-400">Kręgi Mocy i intencje wspierające Twój rozwój.</p>
        </div>
      </div>

      <!-- Przyciski Akcji -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/login"
          class="px-8 py-3 rounded-full bg-gradient-to-r from-amber-500 to-orange-600 text-white font-bold hover:scale-105 transition-transform shadow-lg shadow-amber-500/20"
        >
          Wejdź do Świątyni (Logowanie)
        </a>
        <a
          href="/signup"
          class="px-8 py-3 rounded-full bg-white/10 text-white border border-white/20 hover:bg-white/20 transition-colors font-semibold backdrop-blur-sm"
        >
          Stwórz Konto
        </a>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* Prosta animacja wejścia */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
</style>
